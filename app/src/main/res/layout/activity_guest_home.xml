<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ACTIVITIES.GuestHomeActivity">

    <!-- Background IMAGE (same vibe as other screens, but NO floating card) -->
    <ImageView
        android:id="@+id/ivBackground"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:contentDescription="@null"
        android:scaleType="centerCrop"
        android:src="@drawable/leaves_bg"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Subtle scrim for readability -->
    <View
        android:id="@+id/bgScrim"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#1A000000"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Title -->
    <TextView
        android:id="@+id/tvGuestHomeTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="22dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:text="Guest Home"
        android:textAlignment="center"
        android:textStyle="bold"
        android:textSize="28sp"
        android:letterSpacing="0.02"
        android:textColor="#1B5E20"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Search row -->
    <TextView
        android:id="@+id/tvSearchLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginTop="18dp"
        android:text="Search species"
        android:textStyle="bold"
        android:textSize="13sp"
        android:textColor="#374151"
        app:layout_constraintTop_toBottomOf="@id/tvGuestHomeTitle"
        app:layout_constraintStart_toStartOf="parent" />

    <EditText
        android:id="@+id/etSearchSpecies"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:layout_marginStart="14dp"
        android:layout_marginEnd="20dp"
        android:background="@android:drawable/edit_text"
        android:hint="Type a plant name"
        android:inputType="text"
        android:maxLines="1"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:textColor="#111827"
        android:textColorHint="#6B7280"
        app:layout_constraintTop_toTopOf="@id/tvSearchLabel"
        app:layout_constraintBottom_toBottomOf="@id/tvSearchLabel"
        app:layout_constraintStart_toEndOf="@id/tvSearchLabel"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Category row -->
    <TextView
        android:id="@+id/tvCategoryLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginTop="12dp"
        android:text="category"
        android:textStyle="bold"
        android:textSize="13sp"
        android:textColor="#374151"
        app:layout_constraintTop_toBottomOf="@id/etSearchSpecies"
        app:layout_constraintStart_toStartOf="parent" />

    <Spinner
        android:id="@+id/spCategory"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:layout_marginStart="14dp"
        android:layout_marginEnd="20dp"
        android:background="@android:drawable/edit_text"
        android:spinnerMode="dropdown"
        app:layout_constraintTop_toTopOf="@id/tvCategoryLabel"
        app:layout_constraintBottom_toBottomOf="@id/tvCategoryLabel"
        app:layout_constraintStart_toEndOf="@id/tvCategoryLabel"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- RecyclerView (plants list) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvPlants"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="14dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:clipToPadding="false"
        android:paddingBottom="8dp"
        app:layout_constraintTop_toBottomOf="@id/spCategory"
        app:layout_constraintBottom_toTopOf="@id/btnGuides"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:listitem="@android:layout/simple_list_item_1"
        tools:itemCount="6" />

    <!-- Guides button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnGuides"
        android:layout_width="220dp"
        android:layout_height="48dp"
        android:layout_marginTop="12dp"
        android:text="Guides"
        android:textStyle="bold"
        android:textAllCaps="false"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:backgroundTint="#1B5E20"
        app:cornerRadius="16dp"
        app:layout_constraintTop_toBottomOf="@id/rvPlants"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Bottom buttons row -->
    <LinearLayout
        android:id="@+id/rowBottomButtons"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginTop="14dp"
        android:layout_marginBottom="20dp"
        android:orientation="horizontal"
        android:gravity="center"
        android:weightSum="2"
        app:layout_constraintTop_toBottomOf="@id/btnGuides"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSignIn"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="Sign in"
            android:textStyle="bold"
            android:textAllCaps="false"
            android:textColor="#FFFFFF"
            android:backgroundTint="#1B5E20"
            app:cornerRadius="16dp" />

        <View
            android:layout_width="12dp"
            android:layout_height="1dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnRegister"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="Register"
            android:textStyle="bold"
            android:textAllCaps="false"
            android:textColor="#1B5E20"
            app:strokeColor="#1B5E20"
            app:strokeWidth="1dp"
            app:cornerRadius="16dp" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
